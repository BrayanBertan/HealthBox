import 'package:flutter/material.dart';

class DialogFiltros extends StatelessWidget {
  const DialogFiltros({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Dialog(
      child: Text(''),
    );
  }
}
